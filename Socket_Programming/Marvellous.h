#include<stdio.h>		 // Strandred Input/Output
#include<sys/socket.h>   // For creating the socket
#include<netinet/in.h>   // For Internet Protocall
#include<unistd.h>		 // Import the posix operation
#include<arpa/inet.h>    // Internet operations and IP address manipulations.
#include<stdlib.h>		 // It manages dynamic memory allocation, process control, conversions, and random numbers
#include<string.h>		 // It provides functions to manipulate, copy, search, and measure null-terminated character arrays
#include<libgen.h>      // POSIX-standard header used primarily for parsing pathname strings


